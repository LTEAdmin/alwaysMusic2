import fs from 'fs';
import { fileURLToPath } from 'url';
import { dirname } from 'path';

const agregarJson = (nameAlumno, rutAlumno, cursoAlumno, nivelAlumno) => {
    const estudiante = { nombre: nameAlumno, rut: rutAlumno, curso: cursoAlumno, nivel: nivelAlumno };
    const 
};


